# Chat Server 群聊服务器 — 项目规格（Spec）

> 状态：ready-for-agent · 平台：Linux（VM Ubuntu 18.04）· 语言：C

## Problem Statement

用户刚学完网络编程（TCP 三次握手 / socket 通信 / select·poll·epoll 多路复用），此前学过线程池、文件 IO、IPC。现在需要一个能把这些知识串起来、又能在 Linux VM 上真实跑通的项目，用来巩固知识点，同时作为面试作品。

核心痛点：知识点是零散的，需要一个项目把它们「焊」在一起，尤其是把刚学的 epoll 多路复用真正用起来。

## Solution

做一个 C 语言的多客户端群聊服务器（Chat Server）：

- 多个客户端通过 TCP 连上服务器
- 任一客户端发一条消息，服务器广播给所有在线客户端
- 聊天记录追加写入本地文件（历史落盘）
- 新客户端加入 / 退出时广播通知

架构采用经典模式：**主线程用 epoll 统一监控所有连接（I/O 层），收到消息丢给线程池处理（业务层）**。客户端用 `nc` 或一个极简自写客户端即可测试。

## User Stories

1. 作为客户端用户，我想能通过 TCP 连接到服务器，这样我就能进入群聊
2. 作为客户端用户，我想在连接成功后收到一条欢迎消息，这样我确认自己连上了
3. 作为客户端用户，我想发送一条文本消息，这样其他在线用户能看到我说的话
4. 作为客户端用户，我想收到其他用户发送的消息，这样群聊才有意义
5. 作为客户端用户，我想看到是谁发的消息（消息带来源标识），这样我能区分说话的人
6. 作为客户端用户，我想在别人加入时收到「XX 进入了群聊」的通知，这样我知道谁来了
7. 作为客户端用户，我想在别人离开时收到「XX 离开了群聊」的通知，这样我知道谁走了
8. 作为客户端用户，我发的消息要完整不被截断（正确处理长消息和粘包），这样内容不丢
9. 作为客户端用户，我突然断开连接时服务器要能察觉并清理，这样不会留下「僵尸连接」
10. 作为服务器，我要能同时处理几十上百个客户端连接，这样才算真正的「群」聊
11. 作为服务器，我要在收到异常输入时保持稳定不崩溃，这样服务可靠
12. 作为开发者，我想把「消息处理逻辑」和「网络收发」分开，这样处理逻辑能脱离 socket 单独测试
13. 作为开发者，我想聊天记录按时间顺序追加到文件，这样历史可回溯
14. 作为开发者，我想用 make 一条命令编译，这样构建方便
15. 作为使用者（未来），我想将来能把消息显示到开发板的 LVGL 屏幕上，这样项目能延伸到嵌入式端

## Implementation Decisions

- **语言 / 平台**：C 语言 + POSIX，Linux（先在 VM Ubuntu 18.04 跑通）。注意：VM 里要用**本机 gcc**（编译成 x86_64 在 VM 直接跑），不是给开发板用的 `arm-linux-gcc`。
- **传输层**：TCP（面向连接，保证顺序和可靠），端口用一个固定值（如 8080，可配置）。
- **多路复用**：主线程用 `epoll`（`epoll_create1` / `epoll_ctl` / `epoll_wait`）同时监控 listen fd 和所有 client fd。
- **并发模型**：I/O 与业务分离。主线程只做 epoll 等待和收发；收到完整消息后，把消息交给**线程池**处理（解析、广播、写历史）。这样避免单线程被慢业务阻塞。
- **消息边界（关键决策）**：TCP 是字节流，没有天然消息边界，必须自定协议。决定用**换行符 `\n` 作为消息结束标志**，客户端按行发送，服务器按行读取并做**粘包 / 半包缓冲**处理（每个连接一个接收缓冲区，攒到 `\n` 才视为一条完整消息）。
- **数据结构**：客户端连接列表（含 fd、接收缓冲区、用户标识）；消息结构体（来源、内容、时间）。
- **历史落盘**：聊天记录以文本行追加（append）方式写入一个日志文件。
- **用户标识**：客户端连上后用一个自增 id 或客户端上报的昵称作为标识，广播时带上。

## Testing Decisions

- **测试接缝（seam）**：把「消息处理」做成纯逻辑——输入「一条完整消息 + 在线列表」，输出「要广播给谁 + 要写什么历史」，完全不碰 socket。这是唯一需要的新接缝，放在最高层（业务层），网络层通过它调用业务。
- **单元测试**：测消息解析（粘包 / 半包切分是否正确）、消息格式化（广播文案 / 带来源）。纯函数，好测。
- **集成测试**：真实起服务器，用多个 `nc`（或自写客户端）同时连接，验证广播、进出通知、历史落盘。
- **测试原则**：只测外部可观察行为（连上能收欢迎、发消息全员收到、断开有通知、历史文件有记录），不测 epoll 内部细节。

## Out of Scope

- LVGL 图形界面（留到上开发板阶段再接入）
- 用户认证 / 私聊 / 多房间
- 加密（TLS）
- Windows 平台支持
- 性能压测与调优（先保证正确性）

## Further Notes

- **面试亮点**：这是「epoll + 线程池」经典服务器架构，能讲清楚「为什么用 epoll 不用 select」「I/O 线程与业务线程为什么分开」「TCP 粘包半包怎么处理」三个高频问题。
- **后续扩展路线**：① 加私聊 / 房间 → ② 历史查询 → ③ 接开发板 LVGL 把消息显示到屏幕。
- 项目代码建议放 `Desktop\share\projects\chat-server\`（Windows 写，VM 经 `/mnt/hgfs/share/projects/chat-server/` 编译）。
